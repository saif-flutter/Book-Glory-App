import 'package:flutter/material.dart';

class TheArtScreen extends StatelessWidget {
  const TheArtScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey,
    );
  }
}
